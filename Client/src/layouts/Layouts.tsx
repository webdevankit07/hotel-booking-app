import Header from '../components/Header';

const Layouts = () => {
    return (
        <div className='flex-col min-h-screen flex-'>
            <Header />
            {/* <Footer /> */}
        </div>
    );
};

export default Layouts;
